// ============================================================
// Site configuration
// Loaded before script.js as a plain global (works from file:// too,
// unlike a JSON file that would need fetch()).
// ============================================================
const SITE_CONFIG = {
  version: "1.0.1",
};
